interface:
  display_name: "SemiPulse Sentinel"
  short_description: "Review semiconductor source-chart reports"
  default_prompt: "Use $semipulse-sentinel to review the latest copied source charts and assess whether the source scanner needs a refresh."
